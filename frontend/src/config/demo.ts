export const DEMO_ACCOUNT_EMAIL = 'officer@cpcl-demo.gov.in';

// Fill in the actual demo password your teammate set up. This never leaves
// the frontend build and is only used for the "Quick Demo Access" button
// on the login page — it just fills the form, it doesn't skip the real
// backend login call.
export const DEMO_ACCOUNT_PASSWORD = '123';